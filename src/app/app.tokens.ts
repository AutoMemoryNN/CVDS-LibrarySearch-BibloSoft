/**
 * AppTokens enum is used to define the tokens that
 * enables dependency injection in the application.
 */
export enum AppTokens {
	USERS_DATABASE = 'USERS_DATABASE',
	USER_REPOSITORY = 'USER_REPOSITORY',
	SESSIONS_REPOSITORY = 'SESSIONS_REPOSITORY',
}
